<h1>Add your restaurant</h1>

<%= simple_form_for [@restaurant] do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :phone_number %>
  <%= f.label :category %>
  <%= f.select :category, Restaurant::CATEGORIES, prompt: true %>
  <%= f.submit "add a restaurant", class: "btn btn-primary" %>
<% end %>
